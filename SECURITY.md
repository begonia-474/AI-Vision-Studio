# 报告安全问题

我们认真对待安全问题。**请勿在公开 Issue 中报告安全漏洞。**

## 报告流程

1. 私信联系维护者，或向 `wengshizhuang@gmail.com` 发送邮件，主题以 `[SECURITY]` 开头
2. 请在报告中包含：
   - 漏洞类型与影响面描述
   - 复现步骤（版本、操作系统、最小复现）
   - 可能受影响的数据或功能
3. 我们会在 **48 小时**内确认收到，并尽快评估、修复与发布

## 安全承诺

- API Key 只存于系统凭据管理器，绝不写入 SQLite、日志或远端（内置厂商按 providerId 独立走 keyring）
- SQLite 只存生成历史（厂商 HTTP 请求 / 响应体写库前脱敏，超长文本截断）与用户自添加模型配置（模型 ID / 名称 / 默认参数），不含任何密钥
- 应用无自有服务器，任何第三方服务仅在用户授权下被调用
- 生产构建启用 CSP，asset 协议范围收窄至 `%LOCALAPPDATA%\AIVisionStudio\` 产物目录
- 用户自添加模型仅继承内置模板的参数分区并替换模型 ID，请求直连对应内置厂商端点；请仅使用你信任的模型 ID

## 已修复漏洞

见 [CHANGELOG.md](CHANGELOG.md) 中标注的 `Security` 条目。
